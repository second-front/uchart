{
  "instance": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enables or disables the configMap (is enabled by default)."
      },
      "annotations": {
        "$ref": "metadata.json#/annotations",
        "description": "Provide additional Annotations to the configMap."
      },
      "labels": {
        "$ref": "metadata.json#/labels",
        "description": "Provide additional Labels to the configMap."
      },
      "nameOverride": {
        "type": "string",
        "description": "Override the name suffix that is used for this configMap"
      },
      "data": {
        "type": "object",
        "description": "configMap data content. Helm template enabled.",
        "additionalProperties": {
          "type": "string"
        }
      },
      "binaryData": {
        "type": "object",
        "description": "configMap binary data content.",
        "additionalProperties": {
          "type": "string"
        }
      }
    },
    "oneOf": [
      {
        "required": [
          "data"
        ]
      },
      {
        "required": [
          "binaryData"
        ]
      }
    ]
  }
}
