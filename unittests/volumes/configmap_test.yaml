---
# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: volumes configmap
templates:
  - entrypoint.yaml
values:
  - ../_values/workloads_main_default_container.yaml
tests:
  - it: configmap volumes type should pass
    set:
      volumes:
        configmap:
          type: configMap
          name: mySettings
    asserts:
      - documentIndex: 0
        isKind:
          of: Deployment
      - documentIndex: 0
        equal:
          path: spec.template.spec.volumes[0]
          value:
            name: configmap
            configMap:
              name: mySettings

  - it: configmap volumes type with name template should pass
    set:
      volumes:
        configmap:
          type: configMap
          name: '{{ include "2f.uchart.lib.chart.names.fullname" $ }}-config'
    asserts:
      - documentIndex: 0
        isKind:
          of: Deployment
      - documentIndex: 0
        equal:
          path: spec.template.spec.volumes[0]
          value:
            name: configmap
            configMap:
              name: RELEASE-NAME-config

  - it: configmap volumes type with id reference should pass
    set:
      configMaps:
        mySettings:
          data:
            test: testvalue
      volumes:
        configmap:
          type: configMap
          id: mySettings
    asserts:
      - documentIndex: 0
        isKind:
          of: ConfigMap
      - documentIndex: 1
        isKind:
          of: Deployment
      - documentIndex: 1
        equal:
          path: spec.template.spec.volumes[0]
          value:
            name: configmap
            configMap:
              name: RELEASE-NAME-mySettings

  - it: configmap volumes type with invalid id reference should fail
    set:
      volumes:
        configmap:
          type: configMap
          id: doesNotExist
    asserts:
      - failedTemplate:
          errorMessage: "No configmap found with this id. (volume 'configmap', id 'doesNotExist')"