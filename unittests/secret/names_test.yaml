---
# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: secret names
templates:
  - entrypoint.yaml
values:
  - ../_values/workloads_main_default_container.yaml
tests:
  - it: default should pass
    asserts:
      - hasDocuments:
          count: 1
      - documentIndex: 0
        not: true
        isKind:
          of: Secret

  - it: with disabled secret should pass
    set:
      secrets:
        secret:
          enabled: false
          stringData:
            test: test
    asserts:
      - hasDocuments:
          count: 1
      - documentIndex: 0
        not: true
        isKind:
          of: Secret

  - it: with multiple secrets should pass
    set:
      secrets:
        secret:
          enabled: true
          stringData:
            foo: bar
        secondary:
          enabled: true
          stringData:
            test: test
    asserts:
      - hasDocuments:
          count: 3
      - documentIndex: &FirstSecretDocument 0
        isKind:
          of: Secret
      - documentIndex: *FirstSecretDocument
        isNotNullOrEmpty:
          path: metadata.name
      - documentIndex: &SecondSecretDocument 1
        isKind:
          of: Secret
      - documentIndex: *SecondSecretDocument
        isNotNullOrEmpty:
          path: metadata.name

  - it: default name should pass
    set:
      secrets:
        secret:
          stringData:
            test: test
    asserts:
      - hasDocuments:
          count: 2
      - documentIndex: &SecretDocument 0
        isKind:
          of: Secret
      - documentIndex: *SecretDocument
        equal:
          path: metadata.name
          value: RELEASE-NAME

  - it: with nameOverride should pass
    set:
      secrets:
        secret:
          stringData:
            test: test
          nameOverride: http
    asserts:
      - hasDocuments:
          count: 2
      - documentIndex: &SecretDocument 0
        isKind:
          of: Secret
      - documentIndex: *SecretDocument
        equal:
          path: metadata.name
          value: RELEASE-NAME-http

  - it: multiple names should pass
    set:
      secrets:
        secret:
          stringData:
            test: test
        test:
          stringData:
            test: test
    asserts:
      - hasDocuments:
          count: 3
      - documentIndex: &SecretDocument 0
        isKind:
          of: Secret
      - documentIndex: *SecretDocument
        equal:
          path: metadata.name
          value: RELEASE-NAME-secret
      - documentIndex: &SecretDocument 1
        isKind:
          of: Secret
      - documentIndex: *SecretDocument
        equal:
          path: metadata.name
          value: RELEASE-NAME-test

